# === REQUIRED FOR TESTING ===
DATABASE_URL=sqlite:///./roomly_test.db
SECRET_KEY=roomly-test-secret-key-change-in-production-12345
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
DEBUG=True

# === OPTIONAL (MOCKED FOR TESTING) ===
# Email Settings (will be mocked - no real emails sent)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=test@example.com
SMTP_PASSWORD=test-password

# Push Notification Settings (mocked)
PUSH_ENABLED=false

# Calendar Integration (disabled for testing)
GOOGLE_CALENDAR_ENABLED=false
APPLE_CALENDAR_ENABLED=false

# File Upload (disabled for testing)
FILE_UPLOAD_ENABLED=false
MAX_FILE_SIZE_MB=10
